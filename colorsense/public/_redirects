# Netlify redirects for SPA (Single Page Application)
# This handles client-side routing by redirecting all routes to index.html

# Handle all routes for React Router
/*    /index.html   200

# Optional: Handle API redirects if needed
# /api/*  https://colorsense-backend.onrender.com/api/:splat  200

# Optional: Redirect old paths
# /old-path  /new-path  301
